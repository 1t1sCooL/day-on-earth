apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: day-on-earth
  namespace: default
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: mmalabugin.ru
      http:
        paths:
          - path: /DayOnEarth
            pathType: Prefix
            backend:
              service:
                name: day-on-earth
                port:
                  number: 80
  tls:
    - hosts:
        - mmalabugin.ru
      secretName: main-page-tls
